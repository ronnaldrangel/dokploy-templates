version: "3.8"

services:
  postgres-public:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    labels:
      - "traefik.enable=true"
      - "traefik.tcp.routers.postgres-${DB_DOMAIN}.rule=HostSNI(`${DB_DOMAIN}`)"
      - "traefik.tcp.routers.postgres-${DB_DOMAIN}.entrypoints=websecure"
      - "traefik.tcp.routers.postgres-${DB_DOMAIN}.tls=true"
      - "traefik.tcp.routers.postgres-${DB_DOMAIN}.tls.certresolver=letsencrypt"
      - "traefik.tcp.services.postgres-${DB_DOMAIN}.loadbalancer.server.port=5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
