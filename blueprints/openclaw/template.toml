[variables]
gateway_token = "${password:32}"

[config]
[[config.domains]]
  serviceName = "openclaw"
  port = 8080
  host = "${domain}"

[config.env]
# Identity & Auth
OPENCLAW_GATEWAY_TOKEN = "${gateway_token}"
OPENCLAW_GATEWAY_PORT = 18789
OPENCLAW_ALLOWED_ORIGINS = "*"

# AI Providers
OPENROUTER_API_KEY = "YOUR-API-KEY"

# Network
OPENCLAW_BRIDGE_PORT = 18790
OPENCLAW_GATEWAY_BIND = "lan"
OPENCLAW_DOMAIN = "${domain}"

# Extensions & Plugins
OPENCLAW_PLUGINS = "discord,memory-core"

# System Config (Disable Pairing)
OPENCLAW_CONFIG_JSON = '{"gateway":{"controlUi":{"allowInsecureAuth":true,"dangerouslyDisableDeviceAuth":true}}}'